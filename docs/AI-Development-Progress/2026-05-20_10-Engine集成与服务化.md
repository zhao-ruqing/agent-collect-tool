# Engine集成与服务化 — 2026-05-20 10:00

## 过往实现功能介绍

上次会话完成后端 SQLite→MySQL 迁移、脱敏模块、管理端 API。Agent 端 17 个测试中 1 个失败(test_clear_sent)，LocalQueue 未集成到 Engine，Windows 服务为空 stub，前端 Dashboard 图表为占位符。

---

## 当次已实现功能

- 修复 Agent `test_clear_sent`：LocalQueue::open() 空库时 next_seq 从 0 开始，17/17 测试通过
- LocalQueue 集成到 Engine 主循环：采集→去重→聚合→写入 sled 队列→定时弹出 HTTP 上报，上报失败事件保留在队列
- Engine::new() 新增 queue_path 参数，main.rs 和 service.rs 同步更新
- RawEvent 新增 Deserialize derive，支持队列反序列化
- Windows 服务完整实现：install/uninstall/start/stop/status 通过 sc.exe 管理
- service.rs 实现 Engine 在服务中运行（tokio runtime + 关闭信号处理）
- main.rs 无参数时作为 Windows 服务运行（SCM 启动入口）
- 后端管理端 API 筛选增强：支持 agent_id/tool/model/date_from/date_to 动态 WHERE 查询
- 仪表盘新增 daily_trend 近7天趋势数据、分页列表支持筛选
- 前端 Dashboard 完整实现：ECharts 折线图（每日趋势）+ 饼图（数据总览）+ 统计卡片 + 最近会话表格
- 前端类型定义更新（DailyTrendItem/PaginatedList/ApiResponse），API 层适配后端响应格式
- 后端 12 测试 + Agent 17 测试全部通过，Admin UI 构建成功

## 待实现功能

- Phase 5.2: 后端 CSV 导出接口
- Phase 6.2: 对话记录列表 & 详情页（真实数据 + 筛选）
- Phase 6.3: 代码编辑 Diff 查看器组件 + 行为事件时间线
- Phase 6.4: 客户端管理页（Agent 列表 + 在线状态）
- Phase 7.1: Agent 静默安装器（NSIS）+ 自更新逻辑
- Phase 7.2: 后端请求签名验证、慢查询监控、分区表
- Phase 7.3: JWT 登录 + 路由守卫
- Admin UI 前端未实际启动验证（需要本地 dev server）
